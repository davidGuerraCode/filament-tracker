Transient confirmation — "Photo queued", "Spool saved". Left-border accent only (no full-color fill).

```jsx
<Toast tone="success" title="Queued" message="We'll notify you when it's ready to review." />
```
