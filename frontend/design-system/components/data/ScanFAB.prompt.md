Floating action button that opens the camera/scan flow — always present per the PRD ("floating scan button available from any screen").

```jsx
<ScanFAB onClick={openCamera} />
```

Intentional addition — the PRD calls for a persistent floating scan trigger; no source design system exists to define one.
