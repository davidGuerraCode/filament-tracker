Square (not circular) color chip representing a spool's filament color — square keeps the terminal/pixel aesthetic.

```jsx
<ColorSwatch color="#f2b544" label="Amber" />
```

Intentional addition — no source design system defines this, but the product's core content (filament color) needs a visual chip.
