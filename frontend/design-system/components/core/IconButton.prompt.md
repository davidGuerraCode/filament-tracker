Square icon-only button for toolbars and cards (edit, delete, close glyphs).

```jsx
<IconButton label="Delete spool" variant="ghost"><TrashIcon /></IconButton>
```

Always pass `label` for accessibility (used as aria-label + tooltip title). Sizes sm/md/lg map to 28/36/44px hit targets.
