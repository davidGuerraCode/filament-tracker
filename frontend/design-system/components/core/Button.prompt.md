Primary action trigger, flat terminal styling — no shadows or rounded pills.

```jsx
<Button variant="primary" size="md" onClick={scan}>Scan spool</Button>
```

Variants: primary (solid green), secondary (outlined), ghost (text-only), danger (destructive red). Sizes: sm/md/lg. Pass iconLeft/iconRight for glyphs.
